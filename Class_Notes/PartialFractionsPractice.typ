// command: render

= Practice: Partial Fraction Decomposition

For each rational function:

#enum(
  [Rewrite the function using partial fraction decomposition.],
  [Solve for the constants.]
)

== Problems

#enum(
  [Decompose
  $
  (5x + 7)/((x-1)(x+2))
  $],

  [Decompose
  $
  (3x + 4)/((x-2)(x+1))
  $],

  [Decompose
  $
  (2x + 5)/((x-1)(x+1)(x+3))
  $],

  [Decompose
  $
  (4x - 1)/((x+1)(x-2)(x+4))
  $],

  [Decompose (repeated factor)
  $
  (3x + 1)/((x-1)^2(x+2))
  $],

  [Decompose (irreducible quadratic)
  $
  (2x + 5)/((x-1)(x^2 + 4))
  $]
)